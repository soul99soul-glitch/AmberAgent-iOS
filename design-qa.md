# Amber Watch 文字成果卡核验

日期：2026-09-08。当前唯一设计参考为用户最后选定的深铜文字稿：`docs/reviews/assets/amber-watch-typographic/selected-reference.png`。它替代先前的暖白插画稿；此前“隐藏插画、没有溢出即可通过”的核验不再作为当前设计依据。

## 实际对比

参考原图 1148×1370，与 46mm Watch 的 208×248 pt 画面比例接近。模拟器截屏原始 416×496，工具导出为 671×800；保留真实系统时钟，不绘制替代时钟。

| 区域 | 参考 / 实现 |
| --- | --- |
| 成果卡 | 约 191×118 pt / 约 192×118 pt；顶部约 43 / 42 pt |
| 卡内水平留白 | 约 16 pt / 16 pt |
| 成果文字 | 全宽两行标题、一行摘要；短标题保留两行位置，摘要不会随字数上下跳动 |
| 近期动态 | 无底色文字入口；橙色小点仅表示真实未读状态 |
| 主按钮 | 约 184×40 pt / 约 186×40 pt，底部留白 12 pt |
| 品牌行 | 铜色几何标志与 Amber；独立 reviewer 指出的过大字级已从 20 调到 18 pt |
| 图标与颜色 | 原生 doc + checkmark 组合，深铜渐变和细描边；不再使用插画 |

实际同时查看参考与实现，复核卡片外框、字级、文字左右起点、摘要、按钮及系统时间。中文系统字体行度仍有少量差异，不宣称逐像素一致。

## 状态覆盖

- `home-46mm.jpg`：最新常规字号参考内容，成果卡、动态入口与 CTA 同屏。
- `home-long-46mm.jpg`：真实长度的中文请求预览；仅 UI 省略，底层原文保留。此图早于最后品牌字级/渐变/箭头位置微调，正文宽度与数据规则相同。
- `home-large-46mm.jpg`：大字号改为自然增高的内容流，上方可滚动，CTA 保持可见。
- `home-40mm.jpg`：窄表盘卡片按宽度缩放，无文字或装饰溢出；近期动态需要滚动查看，不能声称三项全部同屏。此图早于最后品牌字级微调。
- `detail-40mm.jpg`：详情有原生返回与完整文本，未把省略后的预览当成全文。
- 同内容的“周末行程”来自隔离 DEBUG fixture，只有显式参数才启用；正常安装启动从真实 iPhone 快照读取。新的小应用成果名取持久化 record.title；旧记录没有可靠名称时不造数据。

独立产品 reviewer 已确认外框、CTA、字级与轻量图标的整体比例；主线程落实其品牌字级、箭头位置和渐变反馈。未将“没有 P0/P1”当作还原标准。

## 验证边界

视觉与比例验收范围是上述原生模拟器画面。大字号和小表盘使用纵向滚动，不保证全内容同屏。真正的触摸/Crown、听写、后台唤醒与真机连接恢复仍须真机交互证据，不能以 fixture 冒充。

本轮逻辑测试、构建、装机及额外聊天回归问题见 `docs/reviews/2026-09-08-amber-watch-typographic.md`，测试与设备结论独立于视觉结论。

final result: passed

真机补充：两端覆盖安装和启动成功，已直接捕获 `device-home-privacy.png`；新布局已出现，卡片文字受系统隐私遮罩保护，未将其当作任务内容或连接稳定性的验收证据。
