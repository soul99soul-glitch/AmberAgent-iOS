# AmberAgent iOS 多维度审查与修复

工作目录：`/Users/arquiel/Downloads/AI/AmberAgent/ios`。审查日期：2026-09-08。

本轮按用户入口追踪到状态、存储、provider 与后台 owner，再复核候选问题。保留开始时已有的工作树改动；并行任务的 Markdown / 流式表格、WebMount 改动不计入本轮修复。没有提交、推送或真机安装。

## 已确认的问题与修复

| 范围 | 实际触发路径与影响 | 本轮处理 |
| --- | --- | --- |
| [会话分支](iosApp/iosApp/IOSConversationStore.swift) | 编辑、删除、切分支或重新生成在异步保存期间切换会话，后续操作可能读取新的 current conversation | 操作捕获原会话 ID；只写原会话，仍选中它时才刷新时间线或继续生成；编辑与截断合并为一次保存 |
| [新建会话](iosApp/iosApp/ChatViewModel.swift) | 首条消息已经出现在 UI，但异步持久化未完成时点击新建，Store 仍把原会话判为空而复用 | 聊天页、首页和深链统一经 ViewModel 检查未落盘消息，必要时强制创建新的 ID；导航成功后才消费待处理的深链输入 |
| [备份恢复并发](iosApp/iosApp/IOSConversationStore.swift) | 生成或后台结果仍待写入时恢复，同 ID 的旧快照可能覆盖恢复内容；清空序列还会令旧 baseline 再次有效 | 恢复前检查全部活动及持久化后台 owner；存储层导入门闩与 epoch 拦截旧快照，等待已有写入完成；旧标题、摘要和图标请求不能回写，清理被恢复会话的旧展示缓存 |
| [本地备份入口](iosApp/iosApp/SyncBackupView.swift) | 关闭远端同步后导入本地备份，恢复预览被放在远端开关内部，无法点击应用 | 恢复预览独立于远端开关；应用期间阻止重复及远端操作重入 |
| [备份上传性能](iosApp/iosApp/SyncBackupView.swift) | 上传刚生成的备份仅需 manifest，却再次执行完整恢复预览解密 | 改用现有 `inspectManifest`，省去重复解密 |
| [全局搜索](iosApp/iosApp/PlaceholderViews.swift) | 页面初次进入重复检索；快速更换关键词后旧检索继续遍历并可能晚到 | 保留单一 query task；捕获关键词并核对取消状态，遍历中及时退出，旧结果不回填 |
| [流式尾行](iosApp/iosApp/ChatCollectionMessageList.swift) | 超长 assistant 回复的上半部仍在屏幕内，但距底部较远，被当作离屏内容停止刷新 | Native 默认路径复用几何可见性；可见尾行持续消费增量，真正离屏才暂停，返回视口恢复；贴底时沿现有判断，避免持续测量超长行的视口相交关系 |
| [Provider 删除](iosApp/iosApp/IOSSharedSettingsStore.swift) | 删除 provider 只清普通 API key，OAuth、Grok cookie 与请求头记录仍残留 | 清理该 provider ID 对应的全部既有凭据存储 |
| [Provider 路由](iosApp/iosApp/IOSGrokWebProvider.swift) | 曾登录 Grok OAuth 的同 ID provider 后来改为普通 OpenAI endpoint，残留 token 仍可能重写请求 | 登录判断、resolver 与 header 注入均核对当前 Grok 配置 |
| [热榜翻译设置](iosApp/iosApp/BoardView.swift) | 翻译关闭仍构造翻译调用；关闭后缓存译名仍显示 | 仅显式开启时调用模型；显示层按开关处理译名，完整缓存保留译文 |
| [热榜筛选缓存](iosApp/iosApp/IOSBoardPersistence.swift) | `focus_only` 将筛选后的结果写回源缓存，之后切回全部无法恢复条目 | 完整源缓存与显示投影分开；保持现有磁盘 schema，设置变化重新投影 |
| [热榜设置返回](iosApp/iosApp/BoardView.swift) | 从设置返回同一个 BoardView，原一次性初始化不再消费新模板、筛选或刷新设置 | 按 Board 相关设置同步；模板独立更新，等待已开始的刷新结束后应用新设置 |
| [热榜 Wi-Fi 设置](iosApp/iosApp/BoardView.swift) | Wi-Fi 开关已持久化但抓取与标题翻译均未消费 | 网络调用前统一检查 Wi-Fi；无 Wi-Fi 时仍应用本地缓存设置，并显示跳过原因 |
| [深度阅读草稿](iosApp/iosApp/DeepReadCreateView.swift) | 清空手工输入或搜索词后，任务创建失败，重试素材丢失 | 任务成功创建后再清空输入 |
| [文件选择](iosApp/iosApp/ChatView.swift) | 注册共享文件授权后等待 Workspace 导入，期间新的选择可以覆盖授权 | 将附件读取绑定本次选择，阻止读到后一个文件 |
| [TTS](iosApp/iosApp/IOSTTSPlayer.swift) | 快速停止并再次试听，旧 utterance 的完成/取消回调把新朗读状态改为停止；离开试听页仍继续播放 | 回调核对当前 utterance；离开 TTS 设置页停止该页试听 |
| [系统权限](iosApp/iosApp/ToolPermissionsView.swift) | 从系统 Settings 返回后权限状态陈旧；直接系统弹窗已拒绝时再次请求无效；通知刷新一直未知 | 页面出现/回到前台刷新实际状态；直接授权已拒绝转系统设置；通知使用异步设置查询，保留 Face ID 等认证重试行为 |
| [记忆输入](iosApp/iosApp/IOSMemoryPersistence.swift) | `confidence` 可写入越界、非有限值；JSON Bool 会桥接成数值 | 在写入前验证有限的 `0...1`，先排除 Bool；无效输入明确失败且不修改记录 |
| [议会取消](iosApp/iosApp/CouncilChatRuntimeView.swift) | 研究请求取消后继续后续搜索/抓取；取消时清空失败席位，归档丢失恢复依据 | 循环在取消时收口；归档保留已经发生的失败席位 |
| [多图生成](iosApp/iosApp/IOSImageGenerationRepository.swift) | 前几张图已保存，后续解码或写入失败，历史记录未创建但文件残留 | 失败时只清理本轮已创建文件，成功插入历史后保留 |

## 复核后保留或限制结论的项目

- Google OAuth 切回品牌默认 endpoint：共享设置层已有明确契约，不增加自定义地址恢复映射。
- 备份批量导入：现有契约是先校验整批再写入，不承诺所有文件 I/O 的事务回滚。本轮处理的是旧写入覆盖恢复内容，不增加通用事务框架。
- 工具声明、未填 API key 的配置状态与终端后端差异：按实际消费入口核对，不能单凭 UI 文案或旧测试预期判为 bug。更新了已过时的终端声明测试断言，以及后台终态旧测试的函数定位范围；后台记录终态后再发布的生产顺序原本正确。
- 目录选择器可选范围、真实 provider 长时间流式返回、系统权限与前后台切换的真机行为，缺少本轮实机证据，不作为已经复现或已经验收的问题。
- `FINAL_ANSWER` mailbox 入队遇到 DAO 写入失败时仍可能缺少可恢复投递记录。这是异常持久化路径的残余风险；本轮没有新增重试或投递日志框架，也不把“只停止唤醒”当作修复完成。
- Wi-Fi 检查是调用前检查，覆盖本次热榜抓取与翻译入口；不承诺请求途中切换网络时的传输层禁止蜂窝流量。旧版本已持久化的筛选缓存无法凭空恢复丢失条目，下一次成功抓取补齐。

## 独立复核

实现后安排其他子代理交叉复核会话/恢复、流式渲染/系统能力/记忆、热榜/议会/图片链路。主线程据此补充恢复期间的排队写入防线、持久化后台 owner 检查、搜索最终取消检查、翻译关闭后的缓存投影。回归还发现新增尾行几何检测影响 24KB 长文刷新；编译对照确认影响后，只在远离底部时读取相交几何，保持 modifier 与消息视图身份稳定。最终再次独立复核并运行原有断言。

## 验证

- KMP：`:ai-core:jvmTest` 109、`:ai-provider-openai:jvmTest` 40、`:core:conversation-storage:jvmTest` 37、`:shared:jvmTest` 27，合计 **213 项通过，0 失败**。
- iOS：iPhone 17 Pro / iOS 26.5 独立模拟器，23 个受影响测试类，**612 项通过，0 失败、0 跳过**。包括 `ChatSwiftUIStreamReplayTests`、`NativeTimelineScrollCoreTests`、`ChatViewportPolicyTests` 三个强制回归类及会话、备份、设置、热榜、议会、图片、权限等关联用例。24KB 长文测试保留原有刷新次数、步长、底部跟随和横向漂移断言。
- 最终 Debug 应用与测试包编译通过，仍有编译警告；`git diff --check` 通过。本轮代码与测试改动共 39 个文件，另有本报告；差异已与开始时的工作树快照区分。
- 最终测试原始结果：[XCTest Result Bundle](/Users/arquiel/Library/Developer/XcodeBuildMCP/workspaces/ios-a3bf81b081aa/result-bundles/test_sim_2026-09-07T23-36-50-948Z_pid64382_e096524d.xcresult)，[统计 JSON](/tmp/amber-review-20260908/final-tests-summary.json)。测试宿主在原共用模拟器上曾启动失败，改用独立模拟器后完成真实执行。
- 已在模拟器完成早期构建与启动。宿主 Mac 处于锁屏状态，交互自动化未获得有效页面切换证据；不宣称完成视觉、滚动手感或真机验收。所有可见动画、材质与排版效果均未主动降级。

测试只针对本轮改动及必要关联路径；通过这些测试不等于整个项目、所有 provider 或全部设备行为均已验证。
