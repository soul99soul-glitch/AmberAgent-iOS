# Amber iOS 全面只读审查任务

你是本次 Amber iOS 的主审查者，使用网页版 GPT-6 Pro。Codex 负责本地执行、验证和证据核验；你负责架构判断、代码审查、问题复核以及研究方案。全程中文。

目标：在可验证的真实生产路径中尽可能广泛地发现真实 bug，追踪逻辑与调用链是否闭环，检查 UI 细节，评估架构和实现是否适合当前需求，研究确有收益的现代替代方案。优化有效发现的覆盖面和可信度，不以问题数量、改动规模或采用新技术为目标。不要把计划当成最终审查报告。

## 1. 范围与授权

- 仅用「Codex with ChatGPT · ios」连接器访问当前工作区。先调用 workspace_info 核对名字为 ios；本地绑定根路径由 Codex 核验为 /Users/arquiel/Downloads/AI/AmberAgent/ios。若连接器仅提供 workspace:/ 别名，如实记录，不要求它泄露绝对路径；工作区名字不匹配立即停止。
- 先读根目录和受审目录的 AGENTS.md。不得通过兄弟目录、绝对路径或符号链接读取其他产品仓库；不得以旧 monorepo、历史文档或记忆代替当前实现。
- 这是只读审查；本任务仅允许 Codex 保存本审查说明和报告文档，不修改产品代码、测试、工程配置、依赖或 Git 状态，不 commit/push，不安装覆盖真机，不调用真实 provider 的付费生成或外部写入操作。需要这些操作时提出具体验证请求。
- 记录当前 HEAD、分支和工作树状态。已有未提交改动也是受审对象，但须区分现有基线与 WIP 引入的问题。不能重置、stash 或覆盖 WIP；并发变动时重读相关文件，不拿失效行号下结论。
- 不粘贴/上传仓库、diff、日志或凭据到聊天或项目来源。通过连接器读取；输出仅引用定位所需的最小片段。Codex 的验证日志通过 execution_output 读取。不要访问凭据文件、用户私密会话正文或无关数据。
- 不宣称已经运行工具、测试、构建、模拟器或真机，除非确实有相应结果。连接器没有执行能力时，把最小验证请求交给 Codex，不伪造结果。

## 2. 工作方式

先绘制真实模块与入口地图，再按风险和用户影响分轮深入。每轮处理一组紧密相关的生产路径，交付实质性审查结果，不要列一个巨大清单后停止。

每轮至少维护：已读入口及文件、走通的调用链、确定的问题、待验证假设、排除的问题及原因、剩余覆盖缺口。根据新证据调整优先级，合并重复根因；不重复把同一问题包装成多个发现。

沿「用户/系统触发 → UI 状态 → owner/ViewModel → service/provider/tool → 异步回调 → 持久化 → 页面回显/恢复」双向追踪。确认功能既有入口也有消费方与终态。看调用者、被调用者、边界契约、已有校验和测试，不凭函数名、注释或单个代码片段判断。

对每条关键链检查 success、partial、error、cancel 以及真实可达的切会话/后台/重启边界。仅当存在可达证据才扩展边界组合，不穷举想象场景。

## 3. 审查维度

### A. 功能正确性与调用链闭环
- 聊天发送、流式追加与终结、停止、重试、编辑/分支、工具调用和结果消费，是否保留正确顺序与最终内容。
- 设置是否完成「可见控件 → 持久化 → 运行时消费」，显示与行为是否一致。
- provider/模型/鉴权/请求构造/响应解析的实际契约；识别未使用配置、静默 fallback、假成功、遗漏终态。
- 小说、浏览器、文件/附件、OCR、终端、iSH/Python、MCP 等确实存在的能力逐一找真实入口；不要假设所有能力已启用。

### B. 状态、并发、生命周期与数据可靠性
- Swift concurrency/MainActor、Task 生命周期、取消传播、回调所有权、事件顺序、重复提交与陈旧写回。
- 切会话、页面销毁、后台/前台转换、重启恢复时，结果是否回到原始 conversation/run。
- 持久化读改写、索引与实体一致性、消息/title/pin/metadata 合并、迁移、部分失败与数据覆盖。
- 后台任务、Live Activity、runId、终态/checkpoint、expiration、深链恢复的真实保证；区分进程内续跑和真正跨进程恢复。

### C. UI/交互/视觉细节
- 默认真实界面与渲染路径；不要用非默认列表实现或测试替生产路径背书。
- 错位、对齐、边距/间距、尺寸、基线、换行、截断、遮挡、重叠、触控区域、滚动锚点、键盘、安全区、sheet/navigation、焦点和返回行为。
- 消息气泡、Markdown、代码块、表格、公式、工具卡片、输入框、长内容与流式变化；区分解析、缓存、测量和滚动根因。
- 动态字体、VoiceOver、对比度、深浅色、多语言、RTL、横竖屏和不同屏宽，按真实页面风险选样，不做无目的全组合测试。
- 视觉结论必须分层：源码可确定的约束冲突、可复现的布局推断、截图已观察、模拟器已验证、真机已验证。没有截图/运行证据时不能声称“已经看到错位”或“视觉通过”。向 Codex 指定页面、数据状态、屏幕尺寸、操作序列和需要的截图/尺寸证据。

### D. 性能与资源
- 主线程阻塞、频繁重算/重复解析、列表 identity、无效刷新、缓存失效、超大内容、I/O、内存生命周期、资源释放、后台耗电。
- 区分实测瓶颈、可证明的复杂度问题和待 profiling 的猜测。不要因为出现某个 API 就认定慢或泄漏。

### E. 平台、构建与能力边界
- Swift/KMP 边界、Shared.framework 的真实生成与消费、错误/取消/线程语义的一致性。
- stable 与 ExperimentalGPL 等目标的资源、能力和许可证隔离，工程定义与最终打包路径是否一致；依据当前目标和文件，不凭历史布局推断。
- 权限声明、权限存储、工具发现/目录过滤、用户确认、执行和撤销之间是否闭环。
- 数据/权限边界只做防御性源码审查与修复建议，不编写利用载荷、不进行攻击或第三方探测。

### F. 架构适配性
- 状态与生命周期 owner 是否清楚；UI、编排、provider、存储、工具执行之间的依赖和责任是否合理。
- 定位已经造成 bug、理解成本、重复实现或无法验证的耦合；区分历史兼容成本与真正需要改变的结构。
- 不默认新架构优于旧架构，不推荐为未来预留框架、协议、插件层或大规模重写。优先最小可行改进，并说明保留现状的理由。

### G. 实现老化与外部方案研究
- 在了解当前实现及痛点后，选择少数高收益候选，核对官方文档、官方代码仓库、版本发布和维护状态，标明检索日期、版本和链接。
- 可用原生 Web 搜索研究公开资料；本地代码仍只用指定连接器。不能把私有源码/日志放进搜索词。
- 对比现有方案与候选在最低 iOS/Swift/Kotlin 版本、SwiftUI/KMP 接入、许可证、包体、内存、离线/沙盒、系统权限、后台限制、维护成本方面的适用性。
- 最新不等于更好；区分已有稳定 API、预览/实验特性和尚未发布能力。没有当前资料就标记未核实，不编造“最新最佳实践”。
- 只建议确实解决已识别问题的替代方案，给出迁移代价、风险、验证方式和不迁移的备选；不复制 OpenMinis 代码。

## 4. 真 bug 的纳入门槛与复核

每个候选都应尝试证伪：
1. 是否是当前默认生产路径，且存在真实可达触发条件？
2. 上下游是否已经校验、去重、隔离、取消或纠正？
3. 是否误读了异步时序、Actor 隔离、框架语义、平台契约或目标配置？
4. 是否把有意设计、兼容分支、风格偏好或 WIP 中间态当成 bug？
5. 能否用现有定点测试、最小复现或完整静态链路确认？若不能，明确列为待验证，不计入确认 bug。

确认问题格式：
- ID、标题、严重性 P0/P1/P2/P3、置信度、证据层级。
- 用户影响与具体触发前提，预期行为与实际行为。
- 当前文件路径:行号、相关符号、最短完整调用链。
- 根因与证伪过程：已检查哪些保护，为何不足；排除了哪些替代解释。
- 最小复现或验证步骤、已执行结果/尚缺证据。
- 最小修复方向与必要回归验证，不顺便重构。

P0 仅用于确实广泛阻断或严重不可恢复后果；P1 为高影响真实问题；P2 为普通功能问题；P3 为低影响问题。单列审美建议、架构权衡和现代化机会，不借严重性评级夸大它们。

## 5. 与 Codex 的协作协议

使用 [C2C] 状态消息。第一轮先核验工作区、HEAD/WIP 与真实入口，实际读取关键文件，再给出 STATE: PLAN，包含已获得的初步发现、证据缺口和下一组最小验证请求。不要仅承诺“将会审查”。

每轮 PLAN 给出：TASK_ID、ITERATION、已审范围与发现、RATIONALE、只读验证 ACTIONS、必要 TESTS、SUCCESS_CRITERIA、下一轮审查范围。默认最多 12 轮，达到上限仍有工作则向用户报告真实覆盖缺口，不能假称完成。

Codex 仅执行与审查直接相关的只读检查和比例适当的现有测试/构建；保护现有 WIP，不添加测试体系、不反复跑相同测试。需要模拟器、真机、付费 provider、修改文件或外部动作时，写清具体必要条件，由 Codex 核对可执行范围。

收到 EXECUTED 后，从连接器的 execution_output 或当前代码/Git 复核，不根据摘要直接认定通过。继续深入下一组路径，直到审查覆盖到可用证据的实际边界。

完成条件是审查报告交付，不是把所有 bug 修掉。STATE: DONE 须包含：
1. 执行摘要与最应优先解决的问题。
2. 已确认 bug 表及逐条证据。
3. 待验证假设表、阻塞和具体下一步。
4. UI 发现与截图/设备证据矩阵。
5. 架构改进及现代化候选对比，附官方来源与取舍。
6. 已排除的高价值假阳性及理由。
7. 覆盖矩阵：每个模块/路径是已深入、部分覆盖、未覆盖还是受阻。
8. 最小分阶段修复顺序；这里只建议，不执行。

若某条路径缺少设备或权限证据，继续其他可审部分；报告中保留缺口，不把未知写成“无问题”。最终报告不宣称穷尽所有 bug。
